<div class="addComment form-inline">
	<%= form_for([@post, @post.comments.build])  do |f| %>
		<%= f.text_area :comment, class: 'comment-textbox' %>
		<%= f.submit 'Contribute', class: 'btn btn-info' %>
	<% end %>
</div>
