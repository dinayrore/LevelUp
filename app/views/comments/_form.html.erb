<div class="addComment">
	<%= form_for([@post, @post.comments.build])  do |f| %>
		<%= f.text_area :comment, class: 'comment-textbox' %><br>
		<br>
		<%= f.submit 'Contribute', class: 'button center' %>
	<% end %>
</div>
